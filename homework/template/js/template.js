console.log("Hello World!");
console.log("functions");

//subtract two numbers and return the result
function subtract(x, y) {
	var difference = x - y;
	return difference;
}

//add two numbers and return the result
function add(x, y) {
	sum = x + y;
	return sum;
}

//divde two numbers and return the result
function divide(x, y) {
	var quotient = x / y;
	return quotient;
}

//mulitply two numbers and return the result
function multiply(x, y) {
	var product = x * y;
	return product;
}

//find the remainder of two varialbes and return the result
function findRemainder(x, y) {
	var remainder = x % y;
	return remainder;
}